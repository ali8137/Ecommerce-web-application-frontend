// import React from 'react'

import Authentication from '../customer/components/authentication/Authentication'

const Login = () => {
  return (
    <div>
      <Authentication isLoginComponent={true} />
    </div>
  )
}

export default Login
